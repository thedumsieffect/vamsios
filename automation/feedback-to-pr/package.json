{
  "name": "feedback-to-pr",
  "version": "0.1.0",
  "private": true,
  "description": "Polls Slack #mcp-feedback and creates GitHub issues that trigger the claude.yml workflow.",
  "type": "module",
  "scripts": {
    "poll": "tsx poll.ts",
    "poll:live": "LIVE=true tsx poll.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}
